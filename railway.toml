[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = "pnpm db:migrate"
startCommand = "if [ \"$SERVICE_ROLE\" = \"worker\" ]; then pnpm worker; else pnpm start; fi"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
