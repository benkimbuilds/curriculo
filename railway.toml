[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
preDeployCommand = "pnpm db:migrate"
startCommand = "sh -c 'pnpm db:migrate && exec pnpm start'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
